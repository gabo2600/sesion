{{>navUsr comites=comites }}

<main class="form container col-sm-9 p-3 ">
    <div class="row mb-3">
        <div class="px-2">
            <label for="">Nombre</label>
            <h4>{{nom}}</h4>
        </div>
        <div class="px-5">
            <label for="">Comite</label>
            <h5>{{comAct}}</h5>
        </div>
        <div class="px-5">
            <label for="">Cargo</label>
            {{#if rol}}
                <h5>Responsable del comitè</h5>
            {{else}}
                <h5>Miembro del comitè</h5>
            {{/if}}
        </div>
    </div>
    
    <h1>Sesiones</h1>
    {{#if rol}}
        <a href="/sesion/{{comAct}}/crear" class="btn btn-pri">Nueva sesiòn</a>
    {{/if}}
    <table class="table">
        <tr>
            <th>Asunto de la sesiòn</th>
            <th>Fecha de inicio</th>
            <th>Fecha de cierre</th>
            <th>responsable</th>
            <th>Acciones</th>
        </tr>
        
    </table>
</main>